{
	"type": "module",
	"devDependencies": {
		"@types/node": "^25.5.0",
		"typescript": "^6.0.2"
	},
	"scripts": {
		"build": "npx tsc && cp src/**.html build/ && cp src/**.css build/",
		"clean": "rm -rf build/ tsconfig.tsbuildinfo"
	},
	"dependencies": {
		"@stdlib/ndarray-flatten": "^0.1.1",
		"@stdlib/stdlib": "^0.3.2"
	}
}
